---
import Layout from "../layouts/Layout.astro";
import json from "../../data/data.json";
import { GlobeIcon, MailIcon, PhoneIcon } from "lucide-react";
import { GitHubIcon } from "../components/icons/GitHubIcon";
import { LinkedInIcon } from "../components/icons/LinkedInIcon";
import Carousel from "../components/Carousel";
---

<Layout title="Portfolio | Nick Golitsyn">
  <main class="" id="home">
    <div class="pt-32 print:hidden"></div>
    <section
      class="w-full space-y-8 print:space-y-6 max-w-screen-md mx-8 md:mx-auto"
    >
      <div class="flex items-center justify-between">
        <div class="flex-1 space-y-1.5">
          <h1 class="text-2xl font-bold text-neutral-950">Nick Golitsyn</h1>
          <p class="text-pretty text-sm text-neutral-500">
            Web Developer focused on building products with extra attention to
            detail.
          </p>
          <p
            class="items-center text-pretty font-mono text-xs text-neutral-500"
          >
            <a
              class="inline-flex gap-x-1.5 align-baseline leading-none hover:underline"
              href={json.contact.locationLink}
              target="_blank"
            >
              <GlobeIcon className="size-3" />
              {json.contact.location}
            </a>
          </p>
          <div class="flex gap-x-2 pt-1 text-sm text-neutral-500 print:hidden">
            {
              json.contact.email ? (
                <button class="size-7 hover:text-neutral-950 hover:bg-neutral-100 outline outline-1 outline-neutral-200 rounded-sm transition-colors">
                  <a href={`mailto:${json.contact.email}`}>
                    <MailIcon className="size-4 m-auto" />
                  </a>
                </button>
              ) : null
            }
            {
              json.contact.tel ? (
                <button class="size-7 hover:text-neutral-950 hover:bg-neutral-100 outline outline-1 outline-neutral-200 rounded-sm transition-colors">
                  <a href={`tel:${json.contact.tel}`}>
                    <PhoneIcon className="size-4 m-auto" />
                  </a>
                </button>
              ) : null
            }
            {
              json.contact.git ? (
                <button class="size-7 hover:text-neutral-950 hover:bg-neutral-100 outline outline-1 outline-neutral-200 rounded-sm transition-colors">
                  <a href={`${json.contact.git}`}>
                    <GitHubIcon className="size-4 m-auto" />
                  </a>
                </button>
              ) : null
            }
            {
              json.contact.linkedIn ? (
                <button class="size-7 hover:text-neutral-950 hover:bg-neutral-100 outline outline-1 outline-neutral-200 rounded-sm transition-colors">
                  <a href={`${json.contact.linkedIn}`}>
                    <LinkedInIcon className="size-4 m-auto" />
                  </a>
                </button>
              ) : null
            }
          </div>
          <div
            class="hidden flex-col gap-x-1 text-sm text-neutral-500 print:flex"
          >
            {
              json.contact.email ? (
                <a href={`mailto:${json.contact.email}`}>
                  <span>{json.contact.email}</span>
                </a>
              ) : null
            }
            {
              json.contact.tel ? (
                <a href={`tel:${json.contact.tel}`}>
                  <span>{json.contact.tel}</span>
                </a>
              ) : null
            }
          </div>
        </div>

        <!-- <Avatar className="size-28">
					<AvatarImage alt={json.name} src={json.avatarUrl} />
					<AvatarFallback>{json.initials}</AvatarFallback>
				</Avatar> -->
      </div>
    </section>
    <section class="mt-10 max-w-screen-md mx-8 md:mx-auto">
      <h2 class="text-xl font-medium mb-2 text-neutral-950" id="about">
        About
      </h2>
      <div class="flex flex-col gap-2 text-neutral-500 text-sm">
        <p>
          I’m a 22-year-old web developer and designer with a passion for
          building visually engaging, intuitive, and high-performing websites.
          Since October 2024, I’ve been part of the team at Strange Tech, where
          I contribute to projects that combine clean design with robust,
          user-focused functionality.
        </p>
        <p>
          My journey began with hands-on experience in college, freelance work,
          and personal projects. In June 2023, I completed a 4-year diploma in
          Web Development at IThub College in Moscow, gaining a solid foundation
          in modern programming languages, UI/UX principles, and industry best
          practices. In early 2024, I further expanded my skillset by completing
          a 10-week Data Analysis bootcamp at Cambridge Spark, adding analytical
          thinking and data-driven decision-making to my developer toolkit.
        </p>
        <p>
          Fluent in both English and Russian, I bring a global perspective to
          every project and pride myself on being adaptable, detail-oriented,
          and always eager to stay on top of the latest design and development
          trends.
        </p>
        <p>
          I am available for select freelance projects-especially those that
          offer creative challenges or opportunities to collaborate with
          like-minded teams. If you’ve got something in mind, feel free to get
          in touch.
        </p>
      </div>
    </section>
    <section class="mt-10">
      <Carousel client:load />
    </section>
    <section class="mt-10 max-w-screen-md mx-8 md:mx-auto">
      <div class="mt-10">
        <h2 class="text-xl font-medium mb-2 text-neutral-950">Skills</h2>
        <div class="flex flex-wrap gap-3">
          {
            json.skills.map((item) => (
              <p class="bg-neutral-100 text-neutral-500 text-xs px-3 py-1 border border-neutral-300 rounded-sm">
                {item}
              </p>
            ))
          }
        </div>
      </div>
      <section
        class="mt-10 print-force-new-page max-w-screen-md mx-8 md:mx-auto"
      >
        <h2 class="text-xl font-medium mb-2 text-neutral-950" id="projects">
          Projects
        </h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
          {
            json.projects.map((item) => (
              <div class="w-full bg-gradient-to-br from-neutral-50 bg-neutral-100 border border-neutral-300 rounded-md px-5 py-4 flex flex-col justify-between gap-5">
                <div class="flex flex-col gap-2">
                  <div class="flex items-center gap-x-2">
                    {item?.url ? (
                      <h3 class="font-medium text-neutral-950 hover:underline">
                        <a target="_blank" href={item?.url}>
                          {item.title}
                        </a>
                      </h3>
                    ) : (
                      <h3 class="font-medium">{item.title}</h3>
                    )}
                    {item?.url && (
                      <span class="relative flex size-2 print:hidden">
                        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-lime-400 opacity-75" />
                        <span class="relative inline-flex rounded-full size-2 bg-lime-500" />
                      </span>
                    )}
                  </div>
                  <p class="text-neutral-500 text-sm">{item.description}</p>
                </div>
                <div class="flex flex-wrap gap-3">
                  {item.stack.map((stackItem) => (
                    <p class="bg-neutral-100 text-neutral-500 text-xs px-3 py-1 border border-neutral-300 rounded-sm">
                      {stackItem}
                    </p>
                  ))}
                </div>
              </div>
            ))
          }
        </div>
      </section>
      <section class="mt-10 max-w-screen-md mx-8 md:mx-auto">
        <h2 class="text-xl font-medium mb-2 text-neutral-950">Experience</h2>
        <div class="flex flex-col gap-5">
          {
            json.experience.map((item) => (
              <div>
                <div class="flex flex-col-reverse items-baseline sm:items-center sm:flex-row sm:justify-between mb-1 md:mb-2">
                  <h3 class="font-medium text-neutral-950">{item.title}</h3>
                  <p class="text-neutral-500 text-sm">{item.date}</p>
                </div>
                <p class="text-neutral-500 text-sm">{item.description}</p>
                <div class="flex flex-wrap gap-2 mt-1 md:mt-2">
                  {item.stack.map((stackItem) => (
                    <p class="bg-neutral-100 text-neutral-500 text-xs px-3 py-1 border border-neutral-300 rounded-sm">
                      {stackItem}
                    </p>
                  ))}
                </div>
              </div>
            ))
          }
        </div>
      </section>
      <section class="mt-10 max-w-screen-md mx-8 md:mx-auto">
        <h2 class="text-xl font-medium mb-2 text-neutral-950">Education</h2>
        <div class="flex flex-col gap-5">
          {
            json.education.map((item) => (
              <div>
                <div class="flex flex-col-reverse items-baseline sm:items-center sm:flex-row sm:justify-between mb-1 md:mb-2">
                  <h3 class="font-medium text-neutral-950">{item.title}</h3>
                  <p class="text-neutral-500 text-sm">{item.date}</p>
                </div>
                <p class="text-neutral-500 text-sm">{item.description}</p>
              </div>
            ))
          }
        </div>
      </section>
    </section>
  </main>
</Layout>
